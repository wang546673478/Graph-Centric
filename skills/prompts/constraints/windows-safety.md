## Windows Safety

This agent runs on Windows. `sed`, `cat`, `grep`, `awk` do NOT exist in cmd.
Use `read_file` and `edit_file` instead of bash for file operations.
Use `findstr` instead of `grep`, `dir` instead of `ls` in bash commands.

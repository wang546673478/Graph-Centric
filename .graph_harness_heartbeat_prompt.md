# 心跳自优化 — Web UI 具体改造

对 Graph-Centric Agent 的 Web 前端 (webui/src/) 进行10轮具体改造。每轮改1-3个文件，必须产生视觉变化。

---

## 10轮任务清单

| 轮次 | 任务 | 参考 |
|------|------|------|
| 1 | 给对话消息卡片加圆角8px和阴影，区分用户消息(右对齐蓝底)和AI消息(左对齐透明) | Kanna 消息气泡 |
| 2 | 侧边栏 RunList 加状态色点：Running=绿, Done=灰, Error=红, Paused=黄 | Hermes Web UI |
| 3 | 顶部工具栏加 token 计数显示和状态 pill 组件 | OpenClaw 工具栏 |
| 4 | 3D 图面板节点标签字体加大到14px，加白色描边，边加流动虚线动画 | Claude Code Agent UI |
| 5 | 设置页面表单改卡片式布局，每个设置项一个卡片，保存按钮固定底部 | Hermes 设置页 |
| 6 | 消息列表自动滚动到底部加平滑动画，新消息淡入效果 | Kanna 动画 |
| 7 | 暗色主题 CSS 变量优化：--bg 改为 #0a0f1a, --bg-panel 改为 #162032, --accent 改为 #60a5fa | HelixUI 配色 |
| 8 | 对话输入框 Composer 加自动高度、发送按钮图标化、placeholder 提示 | OpenChamber 输入 |
| 9 | 移动端响应式：小屏(<768px)侧边栏折叠为汉堡菜单，图面板隐藏 | 通用响应式 |
| 10 | 整体 polish：统一圆角6px、间距16px、hover 过渡0.2s、字体回退链 | 设计系统 |

---

## 每轮建图规则（必须！）

**每轮必须产生至少3个节点和2条DependsOn边：**

```
A (anchor, immutable, Task): 当前问题描述 (如 "Transcript.vue 消息无层次感")
D (goal, Task): 目标效果 (如 "消息卡片有圆角阴影、用户/AI消息视觉区分")
边: D -[DependsOn]-> A

中间节点 (至少1个):
B (Style 或 Component): 具体的样式改动或组件改动
边: B -[DependsOn]-> A, D -[DependsOn]-> B
```

**建图检查清单：**
- [ ] 至少3个节点 (A + D + 至少1个中间节点)
- [ ] A 标记 immutable: true
- [ ] 至少2条 DependsOn 边
- [ ] 节点 summary 非空
- [ ] 禁止建 File 节点（不要列目录！只建 Task/Style/Component）

---

## 执行步骤

1. 建图（A→D，必须按上方的建图规则）
2. Explore: 用 read_file 读 webui/src/ 下的目标文件
3. ProposePatch: 改 CSS 或 Vue 组件
4. SubAgent 用 edit_file/write_file 执行修改
5. 验证 → Review 通过 → 下一轮

---

## 约束

- 每轮只改1-3个文件，不引入新npm依赖
- 保持 Vue3 Composition API + TypeScript
- 使用已有的 CSS 变量体系
- 不改 graph/mod.rs 和 graph/l1.rs
- 第10轮结束自动停止

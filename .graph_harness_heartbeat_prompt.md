# 心跳自优化 — Web UI 功能对标与样式模仿

对 Graph-Centric Agent 的 Web 前端 (webui/src/) 进行10轮改进。

---

## 每轮任务流程

### 步骤1: 研究参考项目
用 Explore 派子代理搜索 GitHub，web_search + web_fetch 查看以下项目的 Web UI：
- OpenClaw (github.com/openclaw/openclaw)
- Kanna (github.com/jakemor/kanna)
- Hermes Web UI (github.com/EKKOLearnAI/hermes-web-ui)
- OpenChamber (github.com/syntax-syndicate/opencode-chamber)
- Claude Code Agent UI (github.com/Ngxba/claude-code-cli-ui)

每轮聚焦一个参考项目，回答：
- 这个项目的 Web UI 有哪些功能模块？
- 它的样式设计有什么特点？
- 有哪些具体的功能或样式可以应用到我们的 webui？

### 步骤2: 对比当前 webui
用 Explore 派子代理读 webui/src/ 下的 Vue 组件，对比参考项目：
- 哪些功能参考项目有、我们没有？
- 哪些样式参考项目做得好、我们可以模仿？

### 步骤3: 建图（强制执行以下规则）
每轮必须产出 Task DAG，不允许建文件目录：

```
A (anchor, Task, immutable: true): 当前 webui 某个具体问题
D (goal, Task): 本轮要达成的具体效果
中间节点 (Task): 实现 D 所需的步骤
边: 全部用 DependsOn，D → 中间节点 → A

示例:
  A: "当前 RunView 对话区域无消息气泡区分"
  D: "消息卡片有角色区分：用户消息右对齐蓝底，AI消息左对齐"
  B1: Task "修改 Transcript.vue 消息样式"
  B2: Task "调整 main.css 消息卡片 CSS 变量"
  边: D→B1→A, D→B2→A, B1→B2
```

**建图检查清单（不满足则本轮失败）：**
- [ ] 所有节点都是 Task 类型（禁止 File/Component/Style/Module）
- [ ] A 节点标记 immutable: true
- [ ] 至少3个节点 (A + D + 至少1个中间)
- [ ] 所有边都是 DependsOn
- [ ] 每个节点 summary 非空

### 步骤4: 执行
ProposePatch → SubAgent 用 edit_file/write_file 改 webui/src/ 下文件 → Review

---

## 约束

- 每轮只改 1-3 个文件
- 不引入新 npm 依赖
- 保持 Vue3 Composition API + TypeScript
- 使用已有 CSS 变量体系 (var(--bg), var(--accent), var(--text) 等)
- 不改 graph/mod.rs 和 graph/l1.rs
- 第10轮结束自动停止

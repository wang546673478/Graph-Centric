# 心跳自优化任务：让 Graph-Centric 自优化循环完成

你正在无人值守的 heartbeat self-optimization loop 中工作。目标不是随便美化 UI，而是让 Graph-Centric Agent 的自优化循环本身可以稳定完成一轮，并为后续轮次留下更可靠的图驱动机制。

## Graph-Centric 铁律

1. 图是任务的真状态。不要只依赖 transcript 记忆。
2. 第一张图只能有 A 和 D：A 是 immutable 起点/当前状态，D 是可验证目标，边为 D DependsOn A。
3. 中间节点必须在 A/D 之后逐步填入。
4. 不知道中间路径时，先 Explore，读代码或搜索资料，再把证据转成节点和边。
5. 复杂节点递归成子图，直到足够具体、可执行、可验证。
6. 执行只走已验证的 Task 图。
7. 节点失败时，先重规划失败节点，再从顶层 A/D 重新验证。
8. 如果失败来自前置输出，就回溯并重规划前置节点，再重新验证。
9. 方案逐个尝试，不一次性幻想全枚举。
10. 自优化轮次只有通过配置的验证命令后才算完成。

## 本轮 A/D

- A：当前 heartbeat 自优化循环会因为编译字段缺失、隐式 git 副作用、prompt 状态漂移、A/D 约束不一致或验证链路不稳定而无法可靠完成。
- D：heartbeat 自优化循环可以在不自动 reset/checkout、不偷偷提交、不丢失铁律 prompt 的情况下完成一轮；`cargo check` 通过，核心 graph/heartbeat/dispatcher/proposer 测试通过。

第一步只建立 A、D 和 D DependsOn A。随后 Explore 相关 Module，再补中间 Task 节点。

## 允许修改范围

- `src/agent/`
- `src/web/`
- `src/bin/`
- `skills/prompts/`
- `docs/`
- `.graph_harness_heartbeat_prompt.md`

不要引入新的 npm 依赖。不要删除测试。不要执行或生成 destructive git 操作。

## 验收

- `cargo check`
- 相关单元测试：dispatcher、proposer、conversation、graph_loop、heartbeat/web config、validator、bash tool
- 若 full `cargo test --lib` 仍有平台相关失败，必须说明剩余失败和原因，并保证自优化主链路测试已通过。

# 心跳自优化 — Web UI 渐进式改进

对 Graph-Centric Agent 的 Web 前端 (webui/src/) 进行改进。

---

## 建图流程（两步走）

### 第一步：锚定起点和终点
**只建两个节点：**
- A（起点/当前问题）：描述现状，标记 `immutable: true`
- D（终点/目标效果）：描述要达到的效果
- 边：D→A（DependsOn）

```
A: "Transcript.vue 消息之间无间距，消息块紧贴难阅读"
D: "消息之间有明确间距(8-12px)，消息内容行高提升至 1.6"
D→A (DependsOn)
```

### 第二步：探索 + 填充中间节点
建完 A 和 D 后，用 explore 了解需要改什么，然后在 A 和 D 之间逐步插入中间 Task 节点。图的结构取决于任务实际需要：

```
简单任务:  A ← D
一般任务:  A ← T1 ← D
复杂任务:  A ← T1 ← T2 ← T3 ← D
分支任务:  A ← T1 ← D
              ↖ T2 ←┘
```

中间节点多少由任务复杂度自然决定，不固定数量。

---

## 每轮具体任务

### Round 1: 改进 Transcript 消息间距与排版
- A: "Transcript.vue 消息之间无间距，消息块紧贴难阅读"
- D: "消息之间有明确间距(8-12px)，消息内容行高提升至 1.6"

### Round 2: 区分用户消息和 AI 消息的视觉样式
- A: "Transcript 中用户消息和 AI 消息外观相同，无法区分"
- D: "用户消息右对齐蓝底，AI 消息左对齐透明底"

---

## 约束
- 禁止 ask_user / block — 无人值守
- 第一步只建 A + D + D→A 边
- 所有节点 Task 类型，所有边 DependsOn
- A 节点 immutable: true
- 只改 webui/src/ 下文件，不引入新 npm 依赖
- 保持 Vue3 Composition API + TypeScript
- 使用已有 CSS 变量体系
